#include <stdio.h>
#include <unistd.h>
#include <cstdlib>

int main () {
   // float x,y;
    //x = atof(argv[0]); y = atof(argv[1]);
    //if (y==0)
	printf("Hello World!!!!");
    //else printf("Wynik dzielenia: %.2f\n",(float)x/y);
    return 0;
	//int argc, char*argv[]
}
